<% content_for :page_title, "Nuevo Usuario" %>

<div class="max-w-2xl mx-auto py-8 px-4">
  <div class="mb-8">
    <h1 class="text-2xl font-bold text-foreground">Nuevo Usuario</h1>
    <p class="mt-1 text-sm text-muted-foreground">
      Completa el formulario para invitar a un nuevo usuario a la empresa.
    </p>
  </div>

  <%= render "form", user: @user, roles: @roles %>
</div>
