<% content_for :page_title, "Editar Usuario" %>

<div class="max-w-2xl mx-auto py-8 px-4">
  <div class="mb-8">
    <h1 class="text-2xl font-bold text-foreground">Editar Usuario</h1>
    <p class="mt-1 text-sm text-muted-foreground">
      Modifica la información del usuario <%= @user.first_name %> <%= @user.first_last_name %>.
    </p>
  </div>

  <%= render "form", user: @user, roles: @roles, current_role_id: @roles.find_by(slug: @current_role)&.id %>
</div>
