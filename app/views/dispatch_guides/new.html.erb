<% content_for :page_title, "Nueva Guia de Remision" %>

<div class="max-w-4xl mx-auto py-8 px-4">
  <div class="mb-8">
    <h1 class="text-2xl font-bold text-foreground">Nueva Guia de Remision</h1>
    <p class="mt-1 text-sm text-muted-foreground">Crea una nueva guia de remision <%= @dispatch_guide.grr? ? "remitente" : "transportista" %>.</p>
  </div>

  <%= render "form", dispatch_guide: @dispatch_guide %>
</div>
